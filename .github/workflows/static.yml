name: Generate and Push Sitemap

# トリガー: main ブランチへの push または手動実行、毎日日本時間 9 時
on:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *' # UTC 0 時 → 日本時間 9 時

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:
      # リポジトリをチェックアウト
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          persist-credentials: true
          clean: true
          fetch-depth: 1

      # sitemap.xml を生成（サブディレクトリ自動検出）
      - name: Generate sitemap.xml
        run: |
          echo '<?xml version="1.0" encoding="UTF-8"?>' > sitemap.xml
          echo '<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">' >> sitemap.xml

          # ルートページ
          echo "  <url><loc>https://hamuzon.github.io/</loc></url>" >> sitemap.xml

          # サブディレクトリを自動取得して URL 追加
          for d in $(find . -maxdepth 1 -type d ! -name ".git" ! -name ".github"); do
            name=$(basename $d)
            echo "  <url><loc>https://hamuzon.github.io/$name/</loc></url>" >> sitemap.xml
          done

          echo '</urlset>' >> sitemap.xml

      # Git 設定
      - name: Configure Git
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

      # sitemap.xml を追加してコミット
      - name: Commit changes
        run: |
          git add sitemap.xml
          git commit -m "Update sitemap.xml" || echo "No changes to commit"

      # push
      - name: Push changes
        run: git push